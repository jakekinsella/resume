import React from 'react';
import styled from '@emotion/styled';

import { colors } from '../../constants';

interface Props {
  title: string;
  subtitle: string;
  children: React.ReactNode
}

function Experience({ title, subtitle, children }: Props) {
  return (
    <div />
  );
}

export default Experience;
